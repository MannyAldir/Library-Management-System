[project]
name = "library"
version = "0.1.0"
description = "Object oriented programming practice and special dunder method implementations"
readme = "README.md"
authors = [
    { name = "Manuel Aldir", email = "mannyaldir@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
requires = ["uv_build>=0.11.9,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.0.0",
]
